services:
  backend:
    build:
      context: ./backend
    container_name: chess_agent_backend
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    env_file:
      - .env
    networks:
      - chess_net
    restart: unless-stopped

networks:
  chess_net:
    driver: bridge
